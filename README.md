# Simple DHCP Server

A lightweight DHCP (Dynamic Host Configuration Protocol) server implementation in Python for educational purposes.

## Features

- 🌐 Assigns IP addresses from a configurable pool
- ⚙️ JSON-based configuration
- 📝 Logging of all DHCP requests
- 🔧 Easy to extend and customize

## Requirements

- Python 3.7+
- Administrator/root privileges (to bind to port 67)

## Installation

1. Clone the repository:
```bash
git clone git@github.com:saifkhlifikh/simple-dhcp-server.git
cd simple-dhcp-server